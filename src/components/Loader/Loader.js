import React from "react"

const Loader = props => {
    return (
        <div style={{position: 'absolute', top: '0px', left: '0px', height: '100vh', width:'100vw', backgroundColor: 'white'}}>
            LOADING...
        </div>
    )
}

export default Loader